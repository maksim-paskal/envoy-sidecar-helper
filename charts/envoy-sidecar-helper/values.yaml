global:
  imageRegistry: ""

image:
  registry: docker.io
  repository: paskalmaksim/envoy-sidecar-helper
  tag: v0.0.5
  pullPolicy: IfNotPresent

rbac:
  serviceAccountName: envoy-sidecar-helper
  annotations: {}
    # "helm.sh/hook": pre-install,pre-upgrade
    # "helm.sh/hook-weight": "-7"
